#include <stdio.h>
#include <stdlib.h>

int main(){
    double x = 0;
    double y = 0;
    double sum = 0;

    x = 3.3;
    y = 7.14;

    sum = x + y;

    printf("x = %lf, y = %lf, x + y = %lf\n", x, y, sum);

    return 0;
}